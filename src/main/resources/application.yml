spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/example_db
    username: postgres
    password: postgres

  liquibase:
    change-log: classpath:/db.changelog/db.changelog-master.xml